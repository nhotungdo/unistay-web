@model List<Unistay_Web.Models.User.ActivityHistory>
@{
    ViewData["Title"] = "Lịch sử hoạt động";
}

<div class="container py-5">
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-header">
                    <h4 class="mb-0">Lịch sử hoạt động</h4>
                    <small class="text-muted">Xem tất cả hoạt động gần đây của bạn</small>
                </div>
                <div class="card-body">
                    @if (!Model.Any())
                    {
                        <div class="alert alert-info">
                            Không có lịch sử hoạt động.
                        </div>
                    }
                    else
                    {
                        <div class="timeline">
                            @foreach (var activity in Model)
                            {
                                <div class="timeline-item mb-4">
                                    <div class="timeline-marker">
                                        @{
                                            string icon = GetActivityIcon(activity.ActivityType);
                                            string badge = GetActivityBadge(activity.ActivityType);
                                        }
                                        <span class="badge @badge">
                                            @Html.Raw(icon)
                                        </span>
                                    </div>
                                    <div class="timeline-content">
                                        <h6 class="mb-1">
                                            @GetActivityLabel(activity.ActivityType)
                                        </h6>
                                        <p class="text-muted small mb-1">
                                            @activity.ActivityDate.ToString("dd/MM/yyyy HH:mm")
                                        </p>
                                        @if (!string.IsNullOrEmpty(activity.Description))
                                        {
                                            <p class="mb-2">@activity.Description</p>
                                        }
                                        @if (!string.IsNullOrEmpty(activity.IpAddress))
                                        {
                                            <small class="text-muted">
                                                <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor"
                                                    stroke-width="2" style="display: inline;">
                                                    <circle cx="12" cy="12" r="10"></circle>
                                                </svg>
                                                Địa chỉ IP: @activity.IpAddress
                                            </small>
                                        }
                                    </div>
                                </div>
                            }
                        </div>

                        @if (ViewBag.HasNextPage)
                        {
                            <nav aria-label="Page navigation">
                                <ul class="pagination justify-content-center">
                                    @if (ViewBag.CurrentPage > 1)
                                    {
                                        <li class="page-item">
                                            <a class="page-link"
                                                href="@Url.Action("ActivityHistory", "Profile", new { page = ViewBag.CurrentPage - 1 })">
                                                Trang trước
                                            </a>
                                        </li>
                                    }
                                    <li class="page-item active">
                                        <span class="page-link">@ViewBag.CurrentPage</span>
                                    </li>
                                    @if (ViewBag.HasNextPage)
                                    {
                                        <li class="page-item">
                                            <a class="page-link"
                                                href="@Url.Action("ActivityHistory", "Profile", new { page = ViewBag.CurrentPage + 1 })">
                                                Trang tiếp theo
                                            </a>
                                        </li>
                                    }
                                </ul>
                            </nav>
                        }
                    }
                </div>
            </div>

            <div class="mt-4">
                <a href="@Url.Action("Index", "Profile")" class="btn btn-outline-secondary">Quay lại hồ sơ</a>
            </div>
        </div>
    </div>
</div>

<style>
    .timeline {
        position: relative;
        padding: 20px 0;
    }

    .timeline-item {
        display: flex;
        position: relative;
        padding-left: 50px;
    }

    .timeline-marker {
        position: absolute;
        left: 0;
        top: 0;
        width: 40px;
        height: 40px;
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .timeline-marker .badge {
        width: 40px;
        height: 40px;
        padding: 0;
        display: flex;
        align-items: center;
        justify-content: center;
        border-radius: 50%;
    }

    .timeline-content {
        flex: 1;
        padding-bottom: 20px;
    }

    .timeline-item:not(:last-child) .timeline-marker::after {
        content: '';
        position: absolute;
        top: 40px;
        left: 50%;
        transform: translateX(-50%);
        width: 2px;
        height: 100%;
        background: #dee2e6;
    }
</style>

@functions {
    private string GetActivityIcon(string activityType)
    {
        return activityType switch
        {
            "ProfileUpdate" => "✏️",
            "AvatarChanged" => "📷",
            "PasswordChanged" => "🔒",
            "EmailVerified" => "✓",
            "ListingCreated" => "🏠",
            "ListingUpdated" => "🔄",
            "ListingDeleted" => "🗑️",
            "BookingCreated" => "📅",
            "ReviewSubmitted" => "⭐",
            "ChatInitiated" => "💬",
            "AccountCreated" => "✨",
            "PasswordReset" => "🔑",
            "PasswordResetRequested" => "📧",
            "NotificationPreferencesUpdated" => "🔔",
            _ => "📝"
        };
    }

    private string GetActivityBadge(string activityType)
    {
        return activityType switch
        {
            "ProfileUpdate" => "badge-info",
            "AvatarChanged" => "badge-info",
            "PasswordChanged" => "badge-warning",
            "EmailVerified" => "badge-success",
            "ListingCreated" => "badge-success",
            "ListingUpdated" => "badge-info",
            "ListingDeleted" => "badge-danger",
            "BookingCreated" => "badge-primary",
            "ReviewSubmitted" => "badge-primary",
            "ChatInitiated" => "badge-secondary",
            "AccountCreated" => "badge-success",
            "PasswordReset" => "badge-warning",
            "PasswordResetRequested" => "badge-warning",
            "NotificationPreferencesUpdated" => "badge-info",
            _ => "badge-secondary"
        };
    }

    private string GetActivityLabel(string activityType)
    {
        return activityType switch
        {
            "ProfileUpdate" => "Cập nhật hồ sơ",
            "AvatarChanged" => "Thay đổi ảnh đại diện",
            "PasswordChanged" => "Đổi mật khẩu",
            "EmailVerified" => "Xác thực email",
            "ListingCreated" => "Tạo bài đăng",
            "ListingUpdated" => "Cập nhật bài đăng",
            "ListingDeleted" => "Xóa bài đăng",
            "BookingCreated" => "Tạo đặt phòng",
            "ReviewSubmitted" => "Gửi đánh giá",
            "ChatInitiated" => "Bắt đầu trò chuyện",
            "AccountCreated" => "Tạo tài khoản",
            "PasswordReset" => "Đặt lại mật khẩu",
            "PasswordResetRequested" => "Yêu cầu đặt lại mật khẩu",
            "NotificationPreferencesUpdated" => "Cập nhật tùy chọn thông báo",
            _ => "Hoạt động"
        };
    }
}