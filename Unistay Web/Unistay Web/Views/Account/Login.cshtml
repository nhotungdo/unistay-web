@{
    ViewData["Title"] = "Đăng nhập";
    Layout = null; // We might want a custom layout or no layout for full screen, but usually keeping default layout is safer for scripts. However, full screen split needs to break out of container.
    // Actually, usually in MVC _Layout.cshtml defines a container. If I want full screen, I might need to override the layout or use a specific one.
    // Given the constraints, I will assume I can just output the HTML and if it's inside a container in _Layout, I might need to use some CSS to break out or just recommend a blank layout.
    // Let's check _ViewStart.cshtml to see what the default layout is.
}

@{
    Layout = ""; // Disabling default layout to have full control over the page structure for this specific modern full-screen design.
}

<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Unistay Web</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/Unistay_Web.styles.css" asp-append-version="true" />
    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@400;500;700;800&family=Plus+Jakarta+Sans:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="~/css/modern-theme.css" asp-append-version="true" />
</head>
<body>
    <div class="auth-container">
        <!-- Decoration Sidebar -->
        <div class="auth-sidebar">
            <div class="shape-blob shape-1"></div>
            <div class="shape-blob shape-2"></div>
            
            <div class="auth-sidebar-content">
                <div class="glass-panel p-5 animate-fade-in" style="background: rgba(255,255,255,0.1); border: 1px solid rgba(255,255,255,0.2);">
                    <div class="mb-4">
                        <svg width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" style="color: var(--primary-600);">
                            <path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"></path>
                            <polyline points="9 22 9 12 15 12 15 22"></polyline>
                        </svg>
                    </div>
                    <h1 class="mb-3 display-4 fw-bold">Tìm nơi ở lý tưởng của bạn.</h1>
                    <p class="h5 fw-normal text-muted mb-4" style="line-height: 1.6;">
                        Kết nối sinh viên và chủ nhà trọ với trải nghiệm tìm kiếm thông minh, an toàn và tiện lợi nhất.
                    </p>
                    
                    <div class="d-flex gap-3 mt-5">
                        <div class="d-flex align-center gap-2">
                            <div style="width: 40px; height: 40px; background: rgba(255,255,255,0.5); border-radius: 50%; display: flex; align-items: center; justify-content: center;">
                                <span style="font-weight: 800; color: var(--primary-600);">10k+</span>
                            </div>
                            <span class="small font-weight-bold opacity-75">Người dùng</span>
                        </div>
                        <div class="d-flex align-center gap-2">
                            <div style="width: 40px; height: 40px; background: rgba(255,255,255,0.5); border-radius: 50%; display: flex; align-items: center; justify-content: center;">
                                <span style="font-weight: 800; color: var(--secondary-500);">5k+</span>
                            </div>
                            <span class="small font-weight-bold opacity-75">Phòng trọ</span>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Main Content -->
        <div class="auth-main">
            
            <div class="auth-card glass-panel p-4 p-md-5 animate-fade-in delay-100">
                <!-- Mobile Logo -->
                <div class="mb-5 d-lg-none">
                    <div class="d-flex align-center gap-2">
                        <div style="width: 40px; height: 40px; background: var(--primary-gradient); border-radius: 10px; display: flex; align-items: center; justify-content: center; color: white;">
                             <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                <path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"></path>
                                <polyline points="9 22 9 12 15 12 15 22"></polyline>
                            </svg>
                        </div>
                        <span style="font-family: var(--font-display); font-size: 1.5rem; font-weight: 800;">Unistay</span>
                    </div>
                </div>

                <div class="mb-5">
                    <h2 class="h1 mb-2">Chào mừng trở lại! 👋</h2>
                    <p class="text-muted">Nhập thông tin đăng nhập của bạn để tiếp tục.</p>
                </div>

                @if (!ViewData.ModelState.IsValid)
                {
                    <div style="background: rgba(239, 68, 68, 0.1); color: var(--danger-color); padding: 1rem; border-radius: var(--radius-md); margin-bottom: 1.5rem;">
                        @foreach (var error in ViewData.ModelState.Values.SelectMany(v => v.Errors))
                        {
                            <p class="m-0 small">@error.ErrorMessage</p>
                        }
                    </div>
                }

                <form asp-action="Login" method="post">
                    @Html.AntiForgeryToken()
                    <input type="hidden" name="returnUrl" value="@ViewData["ReturnUrl"]" />

                    <!-- Email Field -->
                    <div class="mb-4">
                        <label class="text-muted small font-weight-bold mb-1 d-block">EMAIL</label>
                        <div style="position: relative;">
                            <input type="email" name="email" class="form-input" placeholder="name@example.com" style="padding-left: 2.75rem;" />
                            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" style="position: absolute; left: 1rem; top: 50%; transform: translateY(-50%); color: var(--text-light);">
                                <path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"></path>
                                <polyline points="22,6 12,13 2,6"></polyline>
                            </svg>
                        </div>
                    </div>

                    <!-- Password Field -->
                    <div class="mb-4">
                        <div class="d-flex justify-between align-center mb-1">
                             <label class="text-muted small font-weight-bold d-block m-0">MẬT KHẨU</label>
                             <a asp-action="ForgotPassword" class="small text-primary font-weight-bold" style="text-decoration: none;">Quên mật khẩu?</a>
                        </div>
                        <div style="position: relative;">
                            <input type="password" name="password" class="form-input" placeholder="••••••••" style="padding-left: 2.75rem;" />
                            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" style="position: absolute; left: 1rem; top: 50%; transform: translateY(-50%); color: var(--text-light);">
                                <rect x="3" y="11" width="18" height="11" rx="2" ry="2"></rect>
                                <path d="M7 11V7a5 5 0 0 1 10 0v4"></path>
                            </svg>
                        </div>
                    </div>

                    <div class="mb-4">
                         <div class="d-flex align-center gap-2">
                            <input type="checkbox" id="rememberMe" name="rememberMe" style="width: 1.1rem; height: 1.1rem; border-radius: 4px; accent-color: var(--primary-600); cursor: pointer;" />
                            <label for="rememberMe" class="text-muted cursor-pointer" style="user-select: none;">Ghi nhớ đăng nhập trên thiết bị này</label>
                        </div>
                    </div>

                    <button type="submit" class="btn btn-primary w-100 mb-4 py-3" style="font-size: 1.1rem;">
                        Đăng nhập
                    </button>

</form>

                    <div class="position-relative text-center mb-4">
                        <hr class="text-muted opacity-25" />
                        <span class="position-absolute top-50 start-50 translate-middle px-3 small text-muted bg-white">
                            hoặc đăng nhập với
                        </span>
                    </div>

                     <form asp-action="GoogleLogin" method="get">
                        <input type="hidden" name="returnUrl" value="@ViewData["ReturnUrl"]" />
                        <button type="submit" class="btn btn-outline w-100 py-2 text-dark border-2" style="border-color: #E2E8F0;">
                            <svg class="mr-2" width="20" height="20" viewBox="0 0 24 24" fill="currentColor">
                                <path d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z" />
                                <path d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z" />
                                <path d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.07H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.93l2.85-2.22.81-.62z" />
                                <path d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.07l3.66 2.84c.87-2.6 3.3-4.53 6.16-4.53z" />
                            </svg>
                            Google
                        </button>
                    </form>

                <p class="text-center mt-5 text-muted">
                    Chưa có tài khoản? 
                    <a asp-action="Register" class="text-primary font-weight-bold text-decoration-none hover-underline">Đăng ký ngay</a>
                </p>
                
                 <div class="mt-4 text-center">
                    <a href="/" class="small text-muted text-decoration-none d-inline-flex align-items-center gap-1">
                        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                            <path d="M19 12H5M12 19l-7-7 7-7"/>
                        </svg>
                        Trở về trang chủ
                    </a>
                </div>

            </div>
        </div>
    </div>
    
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
</body>
</html>