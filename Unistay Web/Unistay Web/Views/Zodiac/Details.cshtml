@model Unistay_Web.Models.ZodiacSign

@{
    ViewData["Title"] = $"Cung {Model.Name}";
    var horoscope = ViewBag.DailyHoroscope as Unistay_Web.Models.DailyHoroscope;
    
    var elementGradient = Model.Element switch
    {
        Unistay_Web.Models.ZodiacElement.Fire => "linear-gradient(135deg, #f093fb 0%, #f5576c 100%)",
        Unistay_Web.Models.ZodiacElement.Earth => "linear-gradient(135deg, #4facfe 0%, #00f2fe 100%)",
        Unistay_Web.Models.ZodiacElement.Air => "linear-gradient(135deg, #43e97b 0%, #38f9d7 100%)",
        Unistay_Web.Models.ZodiacElement.Water => "linear-gradient(135deg, #fa709a 0%, #fee140 100%)",
        _ => "linear-gradient(135deg, #667eea 0%, #764ba2 100%)"
    };
    
    var elementName = Model.Element switch
    {
        Unistay_Web.Models.ZodiacElement.Fire => "Lửa",
        Unistay_Web.Models.ZodiacElement.Earth => "Đất",
        Unistay_Web.Models.ZodiacElement.Air => "Khí",
        Unistay_Web.Models.ZodiacElement.Water => "Nước",
        _ => ""
    };
    
    var modalityName = Model.Modality switch
    {
        Unistay_Web.Models.ZodiacModality.Cardinal => "Khởi đầu",
        Unistay_Web.Models.ZodiacModality.Fixed => "Cố định",
        Unistay_Web.Models.ZodiacModality.Mutable => "Linh hoạt",
        _ => ""
    };
}

<style>
    .zodiac-detail-hero {
        background: @elementGradient;
        color: white;
        padding: 60px 20px;
        text-align: center;
        border-radius: 30px;
        margin-bottom: 40px;
        box-shadow: 0 10px 40px rgba(0,0,0,0.2);
    }

    .zodiac-detail-hero .symbol {
        font-size: 8rem;
        margin-bottom: 20px;
        display: block;
        animation: pulse 2s ease-in-out infinite;
    }

    .zodiac-detail-hero h1 {
        font-size: 3rem;
        font-weight: 800;
        margin-bottom: 10px;
    }

    .zodiac-detail-hero .english-name {
        font-size: 1.5rem;
        opacity: 0.9;
        font-style: italic;
        margin-bottom: 15px;
    }

    .zodiac-detail-hero .date-range {
        font-size: 1.3rem;
        font-weight: 600;
        padding: 12px 24px;
        background: rgba(255,255,255,0.2);
        border-radius: 25px;
        display: inline-block;
        backdrop-filter: blur(10px);
    }

    .info-cards {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: 20px;
        margin-bottom: 40px;
    }

    .info-card {
        background: white;
        padding: 25px;
        border-radius: 20px;
        box-shadow: 0 5px 20px rgba(0,0,0,0.08);
        transition: all 0.3s ease;
    }

    .info-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 8px 25px rgba(0,0,0,0.12);
    }

    .info-card h4 {
        color: #667eea;
        font-size: 0.9rem;
        font-weight: 600;
        text-transform: uppercase;
        margin-bottom: 10px;
        letter-spacing: 1px;
    }

    .info-card p {
        color: #2d3748;
        font-size: 1.3rem;
        font-weight: 700;
        margin: 0;
    }

    .section {
        background: white;
        padding: 35px;
        border-radius: 20px;
        margin-bottom: 30px;
        box-shadow: 0 5px 20px rgba(0,0,0,0.08);
    }

    .section h2 {
        font-size: 2rem;
        font-weight: 700;
        color: #2d3748;
        margin-bottom: 20px;
        padding-bottom: 15px;
        border-bottom: 3px solid @elementGradient;
        background: @elementGradient;
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
    }

    .description {
        font-size: 1.15rem;
        line-height: 1.8;
        color: #4a5568;
    }

    .traits-list {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
        gap: 12px;
        margin-top: 15px;
    }

    .trait-item {
        padding: 12px 18px;
        background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
        border-radius: 12px;
        font-weight: 600;
        color: #2d3748;
        display: flex;
        align-items: center;
        gap: 10px;
        transition: all 0.3s ease;
    }

    .trait-item:hover {
        transform: translateX(5px);
        box-shadow: 0 3px 10px rgba(0,0,0,0.1);
    }

    .strength {
        background: linear-gradient(135deg, #d4fc79 0%, #96e6a1 100%);
    }

    .weakness {
        background: linear-gradient(135deg, #ffecd2 0%, #fcb69f 100%);
    }

    .compatible-signs {
        display: flex;
        flex-wrap: wrap;
       gap: 15px;
        margin-top: 15px;
    }

    .sign-badge {
        padding: 10px 20px;
        background: @elementGradient;
        color: white;
        border-radius: 20px;
        font-weight: 600;
        transition: all 0.3s ease;
        cursor: pointer;
    }

    .sign-badge:hover {
        transform: scale(1.1);
        box-shadow: 0 5px 15px rgba(0,0,0,0.2);
    }

    .horoscope-box {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        color: white;
        padding: 30px;
        border-radius: 20px;
        margin-bottom: 30px;
        box-shadow: 0 10px 30px rgba(102, 126, 234, 0.3);
    }

    .horoscope-box h3 {
        font-size: 1.8rem;
        margin-bottom: 15px;
        font-weight: 700;
    }

    .horoscope-box .content {
        font-size: 1.2rem;
        line-height: 1.8;
        margin-bottom: 20px;
    }

    .horoscope-scores {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
        gap: 15px;
        margin-top: 20px;
    }

    .score-item {
        text-align: center;
        background: rgba(255,255,255,0.2);
        padding: 15px;
        border-radius: 15px;
        backdrop-filter: blur(10px);
    }

    .score-item .label {
        font-size: 0.9rem;
        opacity: 0.9;
        margin-bottom: 8px;
    }

    .score-item .value {
        font-size: 2rem;
        font-weight: 700;
    }

    .back-button {
        display: inline-block;
        padding: 12px 30px;
        background: #e2e8f0;
        color: #2d3748;
        border-radius: 25px;
        text-decoration: none;
        font-weight: 600;
        transition: all 0.3s ease;
        margin-bottom: 20px;
    }

    .back-button:hover {
        background: #cbd5e0;
        transform: translateX(-5px);
        text-decoration: none;
        color: #2d3748;
    }

    @@keyframes pulse {
        0%, 100% {
            transform: scale(1);
        }
        50% {
            transform: scale(1.05);
        }
    }

    @@media (max-width: 768px) {
        .zodiac-detail-hero .symbol {
            font-size: 5rem;
        }

        .zodiac-detail-hero h1 {
            font-size: 2rem;
        }

        .info-cards {
            grid-template-columns: 1fr;
        }

        .traits-list {
            grid-template-columns: 1fr;
        }
    }
</style>

<div class="container" style="margin-top: 30px;">
    <a href="@Url.Action("Index", "Zodiac")" class="back-button">
        ← Quay lại danh sách
    </a>

    <div class="zodiac-detail-hero">
        <span class="symbol">@Model.Symbol</span>
        <h1>@Model.Name</h1>
        <p class="english-name">@Model.EnglishName</p>
        <div class="date-range">📅 @Model.DateRange</div>
    </div>

    @if (horoscope != null)
    {
        <div class="horoscope-box">
            <h3>🔮 Tử Vi Hôm Nay - @horoscope.Date.ToString("dd/MM/yyyy")</h3>
            <div class="content">@horoscope.Content</div>
            <p><strong>Tâm trạng:</strong> @horoscope.Mood | <strong>Màu may mắn:</strong> @horoscope.LuckyColor | <strong>Số may mắn:</strong> @horoscope.LuckyNumber</p>
            
            <div class="horoscope-scores">
                <div class="score-item">
                    <div class="label">❤️ Tình Yêu</div>
                    <div class="value">@horoscope.LoveScore%</div>
                </div>
                <div class="score-item">
                    <div class="label">💼 Sự Nghiệp</div>
                    <div class="value">@horoscope.CareerScore%</div>
                </div>
                <div class="score-item">
                    <div class="label">💪 Sức Khỏe</div>
                    <div class="value">@horoscope.HealthScore%</div>
                </div>
                <div class="score-item">
                    <div class="label">💰 Tài Chính</div>
                    <div class="value">@horoscope.MoneyScore%</div>
                </div>
            </div>
        </div>
    }

    <div class="info-cards">
        <div class="info-card">
            <h4>Yếu Tố</h4>
            <p>@elementName</p>
        </div>
        <div class="info-card">
            <h4>Tính Chất</h4>
            <p>@modalityName</p>
        </div>
        <div class="info-card">
            <h4>Hành Tinh Chủ Quản</h4>
            <p>@Model.RulingPlanetVietnamese</p>
        </div>
        <div class="info-card">
            <h4>Màu May Mắn</h4>
            <p>@Model.LuckyColor</p>
        </div>
        <div class="info-card">
            <h4>Số May Mắn</h4>
            <p>@Model.LuckyNumber</p>
        </div>
        <div class="info-card">
            <h4>Ngày May Mắn</h4>
            <p>@Model.LuckyDay</p>
        </div>
    </div>

    <div class="section">
        <h2>📖 Mô Tả</h2>
        <p class="description">@Model.Description</p>
    </div>

    <div class="section">
        <h2>✨ Điểm Mạnh</h2>
        <div class="traits-list">
            @foreach (var strength in Model.Strengths)
            {
                <div class="trait-item strength">
                    ✓ @strength
                </div>
            }
        </div>
    </div>

    <div class="section">
        <h2>⚠️ Điểm Yếu</h2>
        <div class="traits-list">
            @foreach (var weakness in Model.Weaknesses)
            {
                <div class="trait-item weakness">
                    ⚡ @weakness
                </div>
            }
        </div>
    </div>

    <div class="section">
        <h2>💕 Cung Hoàng Đạo Tương Thích</h2>
        <div class="compatible-signs">
            @foreach (var compatible in Model.Compatible)
            {
                <span class="sign-badge">@compatible</span>
            }
        </div>
    </div>

    <div class="section">
        <h2>🔮 Ý Nghĩa Biểu Tượng</h2>
        <p class="description">@Model.SymbolMeaning</p>
    </div>

    @if (!string.IsNullOrEmpty(Model.OppositSign))
    {
        <div class="section">
            <h2>⚖️ Cung Đối Cực</h2>
            <p class="description">
                <strong>@Model.OppositSign</strong> - Cung đối cực thường có sức hấp dẫn mạnh mẽ và mang lại sự cân bằng trong mối quan hệ.
            </p>
        </div>
    }
</div>
